---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: gatedkustomizationdeployers.flux.gitops.pro
spec:
  group: flux.gitops.pro
  names:
    kind: GatedKustomizationDeployer
    listKind: GatedKustomizationDeployerList
    plural: gatedkustomizationdeployers
    singular: gatedkustomizationdeployer
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GatedKustomizationDeployer is the Schema for the gatedkustomizationdeployers
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: GatedKustomizationDeployerSpec defines the desired state
              of GatedKustomizationDeployer
            properties:
              commitLimit:
                default: 20
                description: "CloneDepth limits the number of commits to get from
                  the GitRepository. \n This is an optimisation for fetching commits."
                maximum: 100
                minimum: 5
                type: integer
              gate:
                description: Gates are the checks applied before advancing the commit
                  in the GitRepository for the referenced Kustomization.
                items:
                  description: KustomizationGate describes a gate to be checked before
                    updating to the latest commit.
                  properties:
                    healthCheck:
                      description: HealthCheck is a generic URL checker.
                      properties:
                        url:
                          description: URL is a  generic catch-all, query the configured
                            URL and if returns anything other than a 200 response,
                            the check fails.
                          type: string
                      type: object
                    name:
                      description: Name is a string used to identify the gate.
                      type: string
                    scheduled:
                      description: ScheduledCheck is a time-based gate.
                      properties:
                        close:
                          description: hh:mm for the time to "close" the gate at.
                          type: string
                        open:
                          description: 'TODO: These need validation! hh:mm for the
                            time to "open" the gate at.'
                          type: string
                      required:
                      - close
                      - open
                      type: object
                  required:
                  - healthCheck
                  - name
                  - scheduled
                  type: object
                type: array
              interval:
                description: Interval at which to check the GitRepository for updates.
                pattern: ^([0-9]+(\.[0-9]+)?(ms|s|m|h))+$
                type: string
              kustomizationRef:
                description: "The Kustomization resource to track and wait for new
                  commits to be available. \n This will access the GitRepository that
                  is used by the Kustomization."
                properties:
                  name:
                    description: Name of the referent.
                    type: string
                required:
                - name
                type: object
            required:
            - interval
            - kustomizationRef
            type: object
          status:
            description: GatedKustomizationDeployerStatus defines the observed state
              of GatedKustomizationDeployer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
